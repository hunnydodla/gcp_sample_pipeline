name: Lint and Test

on:
  push:
    branches: [ main, create_terraform_files_datasets_tables_buckets_in_gcp ]
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.14.0

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run flake8 (lint)
        run: |
          flake8 parse_transforms/ tests/

      - name: Run tests
        run: |
          pytest tests/